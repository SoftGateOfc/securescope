<!-- RESUMO EXECUTIVO -->
<div class="min-h-screen page-break bg-white text-black flex flex-col relative">
    <!-- CABEÇALHO REUTILIZÁVEL -->
    <%- include('header', { numeroPagina: numeroPaginas?.resumoExecutivo ||
    '06', tituloSecao: 'RESUMO EXECUTIVO' }) %>

    <!-- Conteúdo principal -->
    <div class="flex-1 flex flex-col relative z-0 px-12 py-6">
        <!-- PESSOAS - Esquerda -->
        <div class="flex items-center mb-6">
            <div
                class="w-32 h-32 rounded-full border-[13px] border-blue-500 flex flex-col items-center justify-center bg-white mr-6 relative overflow-hidden"
            >
                <% if (imagens && imagens.pessoas) { %>
                <img
                    src="<%= imagens.pessoas %>"
                    alt="Pessoas"
                    class="w-20 h-20 object-contain"
                />
                <% } else { %>
                <div class="text-blue-500 text-xl mb-1 font-bold">👥</div>
                <% } %>
                <div class="text-xl font-bold text-blue-500 mb-3 -mt-1">
                    <%= dados_modelo?.porcentagem_pilar?.Pessoas || '0' %><span
                        class="text-lg"
                        >%</span
                    >
                </div>
            </div>
            <div>
                <h3 class="text-2xl font-bold text-sky-800 mb-1">
                    PESSOAS - CONFORME
                </h3>
                <p class="text-xl text-sky-900">
                    Cultura de segurança internalizada<br />por todos os times.
                </p>
            </div>
        </div>

        <!-- PROCESSOS - Direita -->
        <div class="flex items-center justify-end mb-6">
            <div class="text-right mr-6">
                <h3 class="text-2xl font-bold text-sky-800">
                    PROCESSOS - CONFORME
                </h3>
                <p class="text-xl text-sky-900">
                    Revisão contínua alinha fluxos a<br />padrões de controles.
                </p>
            </div>
            <div
                class="w-32 h-32 rounded-full border-[13px] border-blue-500 flex flex-col items-center justify-center bg-white relative overflow-hidden"
            >
                <% if (imagens && imagens.processos) { %>
                <img
                    src="<%= imagens.processos %>"
                    alt="Processos"
                    class="w-20 h-20 object-contain"
                />
                <% } else { %>
                <div class="text-blue-500 text-xl mb-1 font-bold">📋</div>
                <% } %>
                <div class="text-xl font-bold text-blue-500 mb-3 -mt-1">
                    <%= dados_modelo?.porcentagem_pilar?.Processos || '0'
                    %><span class="text-lg">%</span>
                </div>
            </div>
        </div>

        <!-- GESTÃO - Esquerda -->
        <div class="flex items-center mb-6">
            <div
                class="w-32 h-32 rounded-full border-[13px] border-blue-500 flex flex-col items-center justify-center bg-white mr-6 relative overflow-hidden"
            >
                <% if (imagens && imagens.gestao) { %>
                <img
                    src="<%= imagens.gestao %>"
                    alt="Gestão"
                    class="w-20 h-20 object-contain"
                />
                <% } else { %>
                <div class="text-blue-500 text-xl mb-1 font-bold">⚙️</div>
                <% } %>
                <div class="text-xl font-bold text-blue-500 mb-3 -mt-1">
                    <%= dados_modelo?.porcentagem_pilar?.Gestao || '0' %><span
                        class="text-lg"
                        >%</span
                    >
                </div>
            </div>
            <div>
                <h3 class="text-2xl font-bold text-sky-800">
                    GESTÃO - CONFORME
                </h3>
                <p class="text-xl text-sky-900">
                    Governança de riscos impulsiona<br />decisões mais seguras.
                </p>
            </div>
        </div>

        <!-- TECNOLOGIA - Direita -->
        <div class="flex items-center justify-end mb-6">
            <div class="text-right mr-6">
                <h3 class="text-2xl font-bold text-sky-800">
                    TECNOLOGIA - CONFORME
                </h3>
                <p class="text-xl text-sky-900">
                    Tecnologias atuais ampliam<br />vigilância e pronta reação.
                </p>
            </div>
            <div
                class="w-32 h-32 rounded-full border-[13px] border-blue-500 flex flex-col items-center justify-center bg-white relative overflow-hidden"
            >
                <% if (imagens && imagens.tecnologia) { %>
                <img
                    src="<%= imagens.tecnologia %>"
                    alt="Tecnologia"
                    class="w-20 h-20 object-contain"
                />
                <% } else { %>
                <div class="text-blue-500 text-xl mb-1 font-bold">💡</div>
                <% } %>
                <div class="text-xl font-bold text-blue-500 mb-3 -mt-1">
                    <%= dados_modelo?.porcentagem_pilar?.Tecnologia || '0'
                    %><span class="text-lg">%</span>
                </div>
            </div>
        </div>

        <!-- INFORMAÇÃO - Esquerda -->
        <div class="flex items-center mb-4">
            <div
                class="w-32 h-32 rounded-full border-[13px] border-blue-500 flex flex-col items-center justify-center bg-white mr-6 relative overflow-hidden"
            >
                <% if (imagens && imagens.informacoes) { %>
                <img
                    src="<%= imagens.informacoes %>"
                    alt="Informações"
                    class="w-20 h-20 object-contain"
                />
                <% } else { %>
                <div class="text-blue-500 text-xl mb-1 font-bold">🔗</div>
                <% } %>
                <div class="text-xl font-bold text-blue-500 mb-3 -mt-1">
                    <%= dados_modelo?.porcentagem_pilar?.Informacao || '0'
                    %><span class="text-lg">%</span>
                </div>
            </div>
            <div>
                <h3 class="text-2xl font-bold text-sky-800 mb-1">
                    INFORMAÇÃO - CONFORME
                </h3>
                <p class="text-xl text-sky-900">
                    Políticas protegem dados e<br />garantem integridade plena.
                </p>
            </div>
        </div>
    </div>

    <!-- Rodapé -->
    <%- include('footer') %>
</div>
